/**
 * Admin page for managing nets (CRUD). Stub — full UI in Task 15.
 */
export function NetsAdminPage() {
  return (
    <div className="max-w-2xl">
      <h1 className="text-xl font-bold text-text-primary mb-4">Manage Nets</h1>
      <p className="text-text-muted text-sm">
        Net management UI coming soon. Use the API directly to create, update, or delete nets.
      </p>
    </div>
  );
}
